services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: relaybox
      POSTGRES_PASSWORD: relaybox_dev
      POSTGRES_DB: relaybox_development
    ports:
      - "5432:5432"
    volumes:
      - relaybox_postgres:/var/lib/postgresql/data

volumes:
  relaybox_postgres:
